# 生活娱乐 - PEDAL：真实物理引擎汽车加速模拟器

---

## 一、Demo 简介

**是什么：** PEDAL 是一个基于真实物理引擎的汽车加速模拟器网页应用。用户可以选择 17 个品牌、100+ 款真实车型，通过油门/刹车踏板体验不同车型的加速性能差异，所有物理参数均基于真实车辆数据推导。

**面向谁：** 汽车爱好者、驾驶模拟游戏玩家、对汽车性能参数感兴趣的用户，以及想直观对比不同品牌车型加速表现的消费者。

**主要功能：**

1. **真实车型数据库** — 涵盖 BMW、Mercedes-Benz、Audi、Porsche、Tesla、比亚迪、蔚来、小鹏、理想、丰田、本田、大众等 17 个品牌共 100+ 款车型，每款车型包含真实的 0-100 加速时间、最高时速、发动机类型、功率、刹车性能等参数。

2. **拟真仪表盘** — 使用 Canvas 多层渲染绘制，包含金属拉丝质感边框、大小刻度线、红色危险区标记、7 段 LED 数字车速显示、档位指示器，燃油车额外配备转速表（含红线区）。指针采用橙红色三角形造型，带阴影和中心金属帽。

3. **真实物理引擎** — 基于车型 0-100 加速时间推导加速度，综合考虑扭矩曲线（峰值型/平缓型/线性型）、空气阻力（与速度平方成正比）、滚动阻力、换挡扭矩中断、软限速因子。燃油车模拟真实变速箱齿比序列（参考 ZF 8HP、Aisin 6AT、Getrag 7DCT 等），电动车模拟电机瞬时扭矩特性并配备动能回收。

![PEDAL 主界面](此处替换为产品主界面截图)

---

## 二、Demo 创作思路

**灵感来源：**

平时看汽车评测视频时，主播们总爱说"百公里加速 X 秒"，但作为普通观众，很难直观感受 3.9 秒和 7.9 秒到底差多少。汽车性能参数表上密密麻麻的数字——马力、扭矩、齿比——对非专业用户来说就像天书。我一直在想，能不能做一个工具，让任何人都能"踩一脚油门"，亲身感受不同车型的加速差异？

**想解决的问题：**

- **参数感知鸿沟：** 消费者看参数表无法直观理解加速性能差异，需要一种交互式体验来弥合"数字"和"感受"之间的鸿沟。
- **电动车 vs 燃油车差异：** 电动车的瞬时扭矩和燃油车的换挡顿挫是两种完全不同的加速体验，文字描述难以传达这种差异。
- **零门槛体验：** 不需要下载 App、不需要高性能电脑，打开网页就能体验。

**为什么做这个方向：**

选择汽车加速模拟器，是因为它在技术深度和娱乐性之间找到了平衡点。物理引擎需要处理扭矩衰减、空气阻力、换挡逻辑等真实力学问题；仪表盘渲染需要追求拟真视觉效果；而最终产物又是一个人人都能玩、都想玩的互动应用。这种"硬核技术 + 大众娱乐"的组合，正好适合用 TRAE 来快速落地——AI 帮我处理繁琐的物理公式推导和 Canvas 绘制代码，我专注于产品体验和参数调校。

---

## 三、Demo 体验地址

**在线体验链接：** [此处替换为部署后的体验链接]

> 提示：选择品牌和车型后，按住油门踏板（或键盘 W 键）加速，松开滑行，按住刹车踏板（或 S 键）减速，按 R 键重置。建议分别尝试 Tesla Model 3 Performance（电动车，3.1 秒破百）和 BMW M340i xDrive（燃油车，4.4 秒破百），感受两种动力类型的加速差异。

---

## 四、TRAE 实践过程

### 4.1 整体开发流程

整个 Demo 从创意到可体验版本，完全使用 TRAE IDE 完成。开发过程分为以下几个阶段：

**阶段一：需求分析与创意落地**

向 TRAE 描述了"做一个汽车加速模拟器"的初步想法，TRAE 帮我梳理了核心功能模块：车型数据系统、物理引擎、仪表盘渲染、交互控制。通过对话明确了技术选型——纯前端单 HTML 文件，Canvas 绘制仪表盘，不引入外部依赖。

**阶段二：车型数据库构建**

让 TRAE 根据真实车辆参数生成车型数据库。每款车型需要包含：0-100 加速时间、最高时速、发动机类型、功率、刹车性能、是否电动车、档位数、换挡强度、扭矩曲线类型、牵引力建立系数。TRAE 一次性生成了 17 个品牌 100+ 款车型的完整数据，覆盖燃油车、混动车、纯电动车三大类型。

**阶段三：物理引擎开发**

这是整个项目最核心的部分。向 TRAE 描述了物理模型的需求：
- 基础加速度从 0-100 加速时间反推
- 扭矩曲线分三种类型（peaky 峰值型/flat 平缓型/linear 线性型）
- 空气阻力与速度平方成正比
- 换挡期间扭矩中断（模拟离合器分离）
- 软限速因子（接近最高时速时平滑衰减）
- 电动车取消换挡逻辑，增加动能回收

TRAE 帮我实现了完整的 `calculateAcceleration`、`updateRPMAndGear`、`simulate0to100` 等核心方法，并加入了换挡状态机、档位滞回（Hysteresis）等真实变速箱特性。

**阶段四：仪表盘 Canvas 渲染**

这是视觉表现的关键。向 TRAE 提出了"拟真但不科幻"的设计要求，TRAE 生成了详细的设计文档（见 `.trae/documents/` 目录），然后逐步实现了多层渲染：
- 外层投影 + 金属外圈渐变
- 玫瑰金拉丝质感边框环（多层渐变 + 高光/暗线）
- 表盘底色径向渐变
- 大小刻度线 + 数字标注 + 红色危险区
- 转速表（燃油车专属，含红线区）
- 指针（三角形造型 + 阴影 + 中心金属帽）
- 7 段 LED 数字车速 + 档位指示器

**阶段五：交互与优化**

添加了油门/刹车踏板（SVG 绘制）、键盘控制（W/S/R）、移动端适配、响应式布局。优化了 Canvas 性能——静态层（表盘底面、刻度、边框）预渲染到离屏 Canvas，每帧只重绘动态层（指针、数字、转速表指针）。

### 4.2 开发关键步骤截图

**截图 1：物理引擎核心代码 — 加速度计算与换挡逻辑**

![物理引擎核心代码](此处替换为截图1)
> 截图说明：TRAE 生成的 `calculateAcceleration` 方法，综合考虑扭矩因子、换挡扭矩中断、软限速因子、空气阻力和滚动阻力。

**截图 2：仪表盘 Canvas 多层渲染**

![仪表盘渲染代码](此处替换为截图2)
> 截图说明：`drawSpeedometerStatic` 方法，使用多层径向/线性渐变模拟金属质感边框和表盘底色。

**截图 3：车型数据库与换挡齿比序列**

![车型数据与齿比](此处替换为截图3)
> 截图说明：`carData` 数据结构与 `GEAR_RATIOS` 常量，参考真实变速箱齿比（ZF 8HP、Aisin 6AT 等）。

**截图 4：TRAE 对话开发过程**

![TRAE 开发对话](此处替换为截图4)
> 截图说明：在 TRAE IDE 中通过自然语言对话，指导 AI 实现仪表盘拟真改造的完整过程。

### 4.3 关键任务对话 Session ID

以下为开发过程中关键任务的对话 Session ID（双击 TRAE 对话即可复制）：

1. **Session ID 1：** `此处替换为 Session ID 1`
   > 任务：物理引擎核心逻辑开发 — 加速度计算、换挡状态机、RPM 模拟

2. **Session ID 2：** `此处替换为 Session ID 2`
   > 任务：仪表盘 Canvas 拟真渲染 — 多层渐变、刻度系统、指针绘制

3. **Session ID 3：** `此处替换为 Session ID 3`
   > 任务：车型数据库构建 — 17 品牌 100+ 车型真实参数生成

---

## 五、开发心得与踩坑总结

### 踩坑经历

1. **0-100 加速时间校准问题：** 最初直接用 `100 / acceleration` 作为基础加速度，但实际模拟的破百时间总是偏快。原因是换挡扭矩中断会损失时间，但基础加速度没有补偿。解决方案是让 TRAE 实现了一个 `simulate0to100` 校准函数——完整模拟换挡状态机跑一遍 0-100，反推出一个 `accelScale` 补偿系数，确保最终模拟时间与车型标称数据一致。

2. **换挡顿挫感不自然：** 最初换挡时 RPM 直接跳变，体验很生硬。后来让 TRAE 加入了换挡过渡动画——升挡时 RPM 在 0.3 秒内平滑下降，降挡时 RPM 先降后升（0.45 秒），并加入档位滞回（Hysteresis）避免频繁换挡。

3. **Canvas 性能优化：** 最初每帧都重绘整个仪表盘（包括静态的刻度、边框），在移动端有明显卡顿。让 TRAE 重构为离屏 Canvas 预渲染静态层，每帧只 `drawImage` + 重绘动态元素，性能大幅提升。

### 开发心得

- **TRAE 最擅长的场景：** 把模糊的需求变成可运行的代码。我说"做一个拟真仪表盘，像真车那样"，TRAE 就能生成多层 Canvas 渲染代码；我说"电动车和燃油车加速感觉不一样"，TRAE 就能实现扭矩曲线差异和动能回收逻辑。
- **人机协作的最佳模式：** AI 负责生成代码骨架和繁琐的数值计算，我负责参数调校和体验打磨。比如扭矩曲线的三个类型（peaky/flat/linear）是我定义的，但具体的数学公式是 TRAE 推导的。
- **设计文档的价值：** TRAE 自动生成的设计文档（`.trae/documents/` 目录）非常有用，它把我的口头需求结构化成了可执行的技术方案，后续开发就是照着文档逐步实现。

---

## 报名帖链接

[此处替换为已通过的社区报名帖链接]
